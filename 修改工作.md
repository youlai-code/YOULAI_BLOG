### 📝 阶段一：部署架构升级 (解决 Git 冲突与自动化)

*目标：实现 `git push` 自动更新代码，同时保护服务器上在线编辑的文章和图片不丢失、不冲突。*

1. **代码与数据分离 (本地操作)**

* [ ] 修改本地 `.gitignore` 文件，添加以下忽略项：
* `public/posts/`
* `public/uploads/`
* `public/config.json`

* [ ] 执行 Git 清理命令，停止追踪上述文件：`git rm -r --cached public/posts` 等。
* [ ] 提交代码并推送到 GitHub。

1. **服务器数据迁移 (服务器操作)**

* [ ] 登录 VPS，在项目目录外创建数据仓库（如 `/var/www/blog_data/`）。
* [ ] 将现有的 `posts`、`uploads` 文件夹和 `config.json` 移动到数据仓库中。
* [ ] 删除项目目录下的原文件夹（为软连接腾出空间）。

1. **配置 GitHub Actions (自动化脚本)**

* [ ] 编写 `.github/workflows/deploy.yml` 文件。
* [ ] 配置 SSH 连接步骤（使用 `appleboy/ssh-action`）。
* [ ] 在脚本中添加 **软连接命令 (Symlink)**：
* `ln -sfn /var/www/blog_data/posts ./public/posts`
* `ln -sfn /var/www/blog_data/uploads ./public/uploads`
* `ln -sf /var/www/blog_data/config.json ./public/config.json`

---

### 阶段二：性能与存储优化 (解决加载慢与费流量)

*目标：减轻 VPS 负担，利用云服务加速图片加载。*

1. **彻底治本方案 (对象存储 OSS/R2)**

* [x] **选型**：注册 Cloudflare R2 (推荐，免流量费) 或 阿里云 OSS / 腾讯云 COS。
* [x] **后端改造**：修改 `server.js` 中的 `/api/upload-image` 接口：
* 引入对应云厂商的 SDK。
* 将 `multer` 保存本地的逻辑改为直接 `put` 到云端 Bucket。
* 接口返回云端 URL (如 `https://r2.your-domain.com/img.jpg`) 而非本地路径。

* [x] **数据迁移**：将 `public/uploads/images` 里的旧图片批量上传到云端，并替换文章中的链接。

---

### 阶段三：内容管理优化 (解决分类与维护)

*目标：让文章管理更标准，支持分类标签，便于迁移。*

1. **引入 YAML Frontmatter 标准**

* [x] **格式改造**：将文章元数据（标题、日期、标签、分类）从 `public/posts/posts.json` 移入 `.md` 文件头部：

```markdown
---
title: 文章标题
category: 技术笔记
tags: [Unity, C#]
---

```

* [x] **后端逻辑重写**：修改 `server.js` 的列表读取逻辑，不再读 JSON，而是遍历 `.md` 文件并解析头部信息（推荐使用 `gray-matter` 库）。

1. **简易分类实现 (不改架构的备选)**

* [ ] 仅在 `posts.json` 每个对象中新增 `"category": "分类名"` 字段。
* [ ] 修改前端 `public/js/index.js`，在 `filterPosts` 函数中增加对 category 的筛选支持。
